# Paddy Crop Disease Frontend

This is a Vite + React frontend for the Paddy Crop Disease Detection model.

## Running Locally

```bash
npm install
npm run dev
```

## Deployment
When deploying to Vercel, make sure the "Root Directory" is set to `frontend`.
